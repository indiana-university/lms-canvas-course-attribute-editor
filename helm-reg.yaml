
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "course-attribute-editor-reg.apps-test.iu.edu"
      paths: []

activeProfiles: "redis,reg"

serviceAccountName: course-attribute-editor-reg

cluster: es-test