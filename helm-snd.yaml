
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-snd

env:
  APP_ENV: snd

ingress:
  enabled: true
  hosts:
    - host: "course-attribute-editor-snd.apps-snd.iu.edu"
      paths: []

activeProfiles: "redis,snd"

serviceAccountName: course-attribute-editor-snd

cluster: es-test